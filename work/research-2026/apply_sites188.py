import json,pathlib,copy
r=pathlib.Path('work/research-2026');p=json.load(open(r/'reviewed.json'));assert not (r/'featured188-before.json').exists();before={};dec=[]
rows=[('9e616aa223d463a0','https://www.yourcabana.com/privacy-policy','The official May 1, 2026 policy identifies Even Health LLC d/b/a Cabana, linking the Cabana brand to its legal operator. The profile retains the filed Cabana Mental Health name. Ownership remains Unknown; no treatment-efficacy claims are inferred.'),('1aa2dd86713009f7','https://www.cambium-usa.com/','Current official site describes advanced materials for extreme environments and explicitly names Cambium Biomaterials, Inc. in its footer. It also announces acquiring SHD Group. This confirms the operating identity, without inferring ownership percentages or merging separate filing IDs.'),('fafa139b261715a8','https://www.cambium-usa.com/','Current official site describes advanced materials for extreme environments and explicitly names Cambium Biomaterials, Inc. in its footer. It also announces acquiring SHD Group. This confirms the operating identity, without inferring ownership percentages or merging separate filing IDs.')]
for i,u,n in rows:
 v=p[i];before[i]=copy.deepcopy(v);v.update(review_outcome='confirmed',status='sourced',website_status='verified',checked_at='2026-09-13',identity_evidence=n,notes=n);v['sources'].append(dict(url=u,label='Official legal-name bridge',claim=n));dec.append(dict(id=i,decision='confirmed',notes=n))
for name,value in [('before',before),('decisions',dec)]: (r/f'featured188-{name}.json').write_text(json.dumps(value,indent=2)+'\n')
for f in [r/'reviewed.json',pathlib.Path('outputs/2026-research-trial/profiles.json')]:f.write_text(json.dumps(p,ensure_ascii=False,indent=2)+'\n')
pathlib.Path('lobbying-map/research/reviewed-2026.json').write_text(json.dumps(p,ensure_ascii=False));print('3 filing-name identities confirmed')
